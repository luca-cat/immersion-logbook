[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "immersion"
version = "0.1.0"

[project.scripts]
immersion = "main:app"

[tool.setuptools]
py-modules = ["main", "database", "tablecreation"]